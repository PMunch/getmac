# Package

version       = "0.1.0"
author        = "PMunch"
description   = "A package to get the MAC address of a local IP address"
license       = "MIT"
srcDir        = "src"



# Dependencies

requires "nim >= 1.0.2"
requires "optionsutils >= 1.0.0"
